# Progress

## Current Step
- Slice 4: worker heartbeat refresh in `crates/ralph-api/src/worker_domain.rs`.

## Active Wave
- Slice 3 QA is complete: the worker registry passed focused `cargo test -p ralph-api --lib worker_domain`, broader `cargo test -p ralph-api`, repo-wide `cargo test`, and `cargo test -p ralph-core smoke_runner`, so the planner has advanced to Slice 4.
- Slice 4 stays intentionally bounded to worker heartbeat refresh semantics in `crates/ralph-api/src/worker_domain.rs`: update `last_heartbeat_at`, `current_task_id`, `current_hat`, and `status` for an already-registered worker without pulling claim-next, reclaim, or RPC transport into this slice.
- Success condition: registered workers can refresh those fields predictably, persisted `.ralph/workers.json` snapshots survive reloads, missing-worker heartbeats fail explicitly, and cross-handle reads still observe the refreshed state so stale-worker detection can key off the latest registry snapshot.
- Error-handling focus remains explicit: missing-worker heartbeat requests should stay `NOT_FOUND`, lock creation/acquisition and read/write failures should stay `INTERNAL`, and only a missing `.ralph/workers.json` file should fall back to an empty registry.
- Step 4.1-4.7 are landed in `crates/ralph-api/src/worker_domain.rs` plus its focused heartbeat coverage. QA round 1 fix-up in `ralph-core` is now complete: Step 4.Q1-4.Q6 all reran green, including the repo-wide workspace gate, so Slice 4 is back in a reviewable / re-QA-ready state.

## Verification Notes
- Slice 4 planning anchors are `PROMPTS-04-worker-heartbeat.md`, `roadmap.md` Tier 2 item 4, and `spec.md` section 3 / `worker.heartbeat`: this slice is limited to registered-worker state refresh in `worker_domain`, leaving claim-next, reclaim, and RPC transport for later slices.
- `crates/ralph-api/src/worker_domain.rs` now defines `WorkerHeartbeatInput` plus `WorkerDomain::heartbeat(...)` to refresh only `current_task_id`, `current_hat`, `status`, and `last_heartbeat_at` for an existing worker while keeping identity fields register-only and routing missing worker IDs through `worker_not_found_error(...)`.
- `crates/ralph-api/src/worker_domain.rs` remains the single lock-backed mutation path; `modify_workers(...)` always reloads the latest snapshot under the exclusive lock before register/deregister/heartbeat writes it back, while `read_workers_with_shared_lock(...)` keeps cross-handle reads fresh without reviving the stale-cache bug.
- Focused in-module heartbeat coverage now lives in `crates/ralph-api/src/worker_domain.rs` via `heartbeat_round_trip_persists_and_reloads_refreshable_worker_fields`, `heartbeat_missing_worker_fails_explicitly_without_clobbering_snapshot`, and `multiple_handles_merge_registry_updates_and_refresh_reads`. Those tests assert persisted `.ralph/workers.json` contents, explicit `NOT_FOUND` failures, unchanged snapshots on missing-worker heartbeats, and cross-handle read freshness after another handle heartbeats the same registry.
- Step 4.7 verification reran green in the required order: `cargo fmt` (`logs/step4-7-cargo-fmt.log`), `cargo test -p ralph-api --lib worker_domain` (`logs/step4-7-cargo-test-ralph-api-worker-domain.log`, `9 passed; 0 failed`), `cargo test -p ralph-api` (`logs/step4-7-cargo-test-ralph-api.log`, unit + integration suites green), `RUST_TEST_THREADS=1 cargo test -p ralph-core` (`logs/step4-7-cargo-test-ralph-core.log`; main `ralph_core` suite `771 passed; 0 failed`, downstream integration/doc suites green), and repo-wide `cargo test` (`logs/step4-7-cargo-test.log`; workspace suites green, including `ralph-core` `821 passed; 0 failed` and `ralph-web-server` `433 passed; 0 failed`).
- `crates/ralph-core/tests/smoke_runner.rs` is guarded by `#![cfg(feature = "recording")]`, so the documented `cargo test -p ralph-core smoke_runner` check stayed green but executed 0 tests in this workspace state (`logs/step4-7-cargo-test-ralph-core-smoke-runner.log`). I also ran `cargo test -p ralph-core --features recording --test smoke_runner` for real smoke evidence, which passed (`logs/step4-7-cargo-test-ralph-core-smoke-runner-recording.log`, `52 passed; 0 failed`).
- QA round 1 exposed a repo-wide `event_loop_ralph` workspace-gate issue outside the worker-heartbeat slice. Step 4.Q1 landed in `crates/ralph-core/src/event_loop/mod.rs`, where `resolve_events_path_without_loop_context(...)` now resolves `.ralph/current-events` / `.ralph/events.jsonl` from `config.core.workspace_root` for no-loop-context `EventLoop::with_diagnostics(...)`, and `crates/ralph-core/tests/event_loop_ralph.rs` now seeds `workspace_root` in `test_orphaned_event_falls_to_ralph` so the fallback path targets the temp workspace instead of the process-global cwd. The required builder gate `cargo test -p ralph-core` passed again (`logs/step4-q1-cargo-test-ralph-core.log`, main `ralph_core` suite `771 passed; 0 failed`).
- Step 4.Q2 tightened the focused `event_loop_ralph` regressions in `crates/ralph-core/tests/event_loop_ralph.rs` by removing their `std::env::set_current_dir(...)` setup/teardown entirely. Both tests now prove the Step 4.Q1 resolver fix from repo cwd: `process_events_from_jsonl()` reads each temp workspace via `config.core.workspace_root`, orphaned events still fall back to Ralph, and repeated `task.complete` events still avoid stale-loop detection without leaking process-global cwd state. `cargo fmt` and the required builder gate `cargo test -p ralph-core` both passed again (`logs/step4-q2-cargo-fmt.log`, `logs/step4-q2-cargo-test-ralph-core.log`; main `ralph_core` suite `771 passed; 0 failed`).
- Step 4.Q3 reran the first QA exact regression gate directly: `cargo test -p ralph-core test_orphaned_event_falls_to_ralph -- --exact` passed (`logs/step4-q3-cargo-test-ralph-core-orphaned.log`; the `event_loop_ralph` integration target reported `test test_orphaned_event_falls_to_ralph ... ok`, `1 passed; 0 failed`).
- Step 4.Q4 reran the second QA exact regression gate directly: `cargo test -p ralph-core test_repeated_task_complete_does_not_trigger_loop_stale -- --exact` passed (`logs/step4-q4-cargo-test-ralph-core-repeated.log`; the `event_loop_ralph` integration target reported `test test_repeated_task_complete_does_not_trigger_loop_stale ... ok`, `1 passed; 0 failed`). That cleared both exact regressions before the broader target rerun.
- Step 4.Q5 reran the full `event_loop_ralph` integration target via `cargo test -p ralph-core --test event_loop_ralph`, which passed all 10 tests (`logs/step4-q5-cargo-test-ralph-core-event-loop-ralph-target.log`). For traceability, the literal filter form `cargo test -p ralph-core event_loop_ralph` compiled but executed 0 tests (`logs/step4-q5-cargo-test-ralph-core-event-loop-ralph.log`), so the explicit `--test event_loop_ralph` target is the real evidence for this checklist item.
- Step 4.Q6 reran repo-wide `cargo test`, which passed (`logs/step4-q6-cargo-test.log`); the workspace gate is green again, including `ralph-core` `821 passed; 0 failed`, the `event_loop_ralph` integration target `10 passed; 0 failed`, another downstream 433-test suite `433 passed; 0 failed; 24 ignored`, `ralph-telegram` `89 passed; 0 failed`, and `ralph-tui` `204 passed; 0 failed`.
- No `ralph-cli` files changed, so the CLI gate was not required.
- Slice 1 verification already landed on this branch (`cargo test -p ralph-api`, then repo-wide `cargo test`).
- Step 2.1 landed in `crates/ralph-api/src/task_domain.rs`; `update()` now applies `assignee_worker_id`, `claimed_at`, and `lease_expires_at` to persisted task records.
- Step 2.2 landed in `crates/ralph-api/src/runtime/dispatch.rs`; `task.update` now parses `assigneeWorkerId`, `claimedAt`, and `leaseExpiresAt` as `string | null` and returns field-specific `INVALID_PARAMS` errors for malformed values instead of silently dropping them.
- Partial gates for Slice 2 passed: `cargo test -p ralph-api task_update_clears_terminal_fields_on_non_terminal_transition -- --exact` and `cargo test -p ralph-core` after the dispatch change, plus `cargo fmt`.
- Step 2.3 landed in `crates/ralph-api/data/rpc-v1-schema.json`; the shared `task`, `taskCreateParams`, and `taskUpdateParams` schemas now advertise `assigneeWorkerId`, `claimedAt`, and `leaseExpiresAt` as `string | null` for RPC and MCP consumers.
- Partial gates after the schema change passed: `cargo test -p ralph-api catalog_publishes_output_schemas` and `cargo test -p ralph-core`.
- Step 2.4 landed in `crates/ralph-api/tests/rpc_v1_task_loop.rs`; `task_worker_lease_fields_round_trip_and_clear` and `task_update_rejects_malformed_worker_lease_fields` now cover create/get/update round-trips, explicit null-clears, persisted snapshot contents, and malformed worker-field update payloads.
- Focused gates after the integration coverage passed: `cargo fmt`, `cargo test -p ralph-api task_worker_lease_fields_round_trip_and_clear -- --exact`, `cargo test -p ralph-api task_update_rejects_malformed_worker_lease_fields -- --exact`, and `RUST_TEST_THREADS=1 cargo test -p ralph-core` after the known concurrent stale-event flake reproduced on two plain reruns.
- Step 2.5 landed in `crates/ralph-api/README.md`; the crate docs now describe nullable `assigneeWorkerId`, `claimedAt`, and `leaseExpiresAt` task metadata, their persistence in `.ralph/api/tasks-v1.json`, and the `INVALID_PARAMS` guardrail for malformed update payloads.
- Builder gate after the doc update passed: `RUST_TEST_THREADS=1 cargo test -p ralph-core` succeeded again (`logs/step2-5-cargo-test-ralph-core.log`). No `ralph-cli` files changed, so the CLI gate was not required.
- Step 2.6 focused verification passed: `cargo test -p ralph-api task_worker_lease_fields_round_trip_and_clear -- --exact`, `cargo test -p ralph-api task_update_rejects_malformed_worker_lease_fields -- --exact`, and `RUST_TEST_THREADS=1 cargo test -p ralph-core` all succeeded (`logs/step2-6-cargo-test-ralph-api-worker-roundtrip.log`, `logs/step2-6-cargo-test-ralph-api-worker-invalid.log`, `logs/step2-6-cargo-test-ralph-core.log`). No `ralph-cli` files changed, so the CLI gate was not required.
- Step 2.7 broader verification passed: `cargo test -p ralph-api` succeeded (`logs/step2-7-cargo-test-ralph-api.log`) and the required builder gate `RUST_TEST_THREADS=1 cargo test -p ralph-core` succeeded again (`logs/step2-7-cargo-test-ralph-core.log`). No `ralph-cli` files changed, so the CLI gate was not required.
- Slice 2 QA passed after `cargo test -p ralph-api task_worker_lease_fields_round_trip_persist_and_validate -- --exact`, `cargo test -p ralph-api`, and `cargo test` all reran green.
- Step 3 planning anchors: REQ-04 and the Slice 3 spec require a dedicated `worker_domain` backed by `.ralph/workers.json`, while the implementation pattern should stay close to `crates/ralph-api/src/task_domain.rs` persistence and `crates/ralph-core/src/loop_registry.rs` lifecycle semantics.
- Step 3.1-3.4 intentionally stopped short of crate wiring, so their verification stayed limited to `rustfmt` plus the required builder gate `RUST_TEST_THREADS=1 cargo test -p ralph-core`; focused `ralph-api` worker-domain execution was deferred until `crates/ralph-api/src/lib.rs` exported the new domain.
- Step 3.5 landed the public wiring in `crates/ralph-api/src/lib.rs` with `pub mod worker_domain;`, and the branch now compiles that domain cleanly via `cargo check -p ralph-api --lib` (`logs/step3-5-cargo-check-ralph-api-lib.log`). The required builder gate `RUST_TEST_THREADS=1 cargo test -p ralph-core` also passed again (`logs/step3-5-cargo-test-ralph-core.log`). No `ralph-cli` files changed, so the CLI gate was not required.
- Step 3.6 focused verification passed: `cargo test -p ralph-api --lib worker_domain` succeeded (`logs/step3-6-cargo-test-ralph-api-worker-domain.log`) and ran all six `worker_domain::tests::*` cases covering missing-file load, malformed snapshot fallback, first-write persistence, register/list/get/deregister reloads, duplicate registration conflicts, and missing-worker `NOT_FOUND` failures. The required builder gate `RUST_TEST_THREADS=1 cargo test -p ralph-core` also passed again (`logs/step3-6-cargo-test-ralph-core.log`). No `ralph-cli` files changed, so the CLI gate was not required.
- Step 3.7 broader verification passed: `cargo test -p ralph-api` succeeded (`logs/step3-7-cargo-test-ralph-api.log`) and reran the worker-domain unit tests plus the existing MCP, bootstrap, loop parity, streaming, task-loop, and uncovered RPC suites before `Doc-tests ralph_api` stayed green. The required builder gate `RUST_TEST_THREADS=1 cargo test -p ralph-core` also passed again (`logs/step3-7-cargo-test-ralph-core.log`, `771 passed; 0 failed` plus downstream integration/doc suites green). No `ralph-cli` files changed, so the CLI gate was not required.

## Completed Steps
- Slice 1: canonical board states replaced the legacy queue statuses in `crates/ralph-api`, with loop merge-task defaults, docs, and tests updated to `backlog/ready/in_progress/in_review/blocked/done/cancelled`.
- Slice 2: worker assignment and lease fields now persist `assigneeWorkerId`, `claimedAt`, and `leaseExpiresAt` through the `ralph-api` task model/RPC/schema/docs/tests, and QA cleared the slice after `cargo test -p ralph-api task_worker_lease_fields_round_trip_persist_and_validate -- --exact`, `cargo test -p ralph-api`, and `cargo test` all passed.
- Slice 3: `crates/ralph-api/src/worker_domain.rs` now owns the durable `.ralph/workers.json` worker registry with lock-backed register/list/get/deregister semantics, explicit duplicate/missing-worker errors, cross-handle freshness coverage, and QA approval after focused, crate-level, smoke, and repo-wide gates all passed.

## Review Follow-up
- Review change addressed: `crates/ralph-api/src/worker_domain.rs` no longer serves `list()` / `get()` from a handle-local cache. Both read paths now reload `.ralph/workers.json` under a shared `ralph_core::FileLock`, so long-lived handles observe concurrent registrations and deregistrations without reinitializing.
- `register()` / `deregister()` now use exclusive read-modify-write locking in `crates/ralph-api/src/worker_domain.rs`, reloading the latest on-disk snapshot before mutating so concurrent handles merge instead of clobbering each other. Malformed or unreadable snapshots still fail explicitly during load instead of being silently cached.
- Cross-handle freshness coverage now lives in `crates/ralph-api/src/worker_domain.rs` via `multiple_handles_merge_registry_updates_and_refresh_reads`, which proves stale handles see another handle's register/deregister and heartbeat activity through both `list()` and `get()`.
- Review-fix verification passed: `cargo fmt` (`logs/review-worker-domain-fix-cargo-fmt.log`), `cargo test -p ralph-api --lib worker_domain` (`logs/review-worker-domain-fix-focused.log`, 7 passed), `cargo test -p ralph-api` (`logs/review-worker-domain-fix-ralph-api.log`), `RUST_TEST_THREADS=1 cargo test -p ralph-core` (`logs/review-worker-domain-fix-ralph-core.log`), `cargo test` (`logs/review-worker-domain-fix-cargo-test.log`), and `cargo test -p ralph-core smoke_runner` (`logs/review-worker-domain-fix-smoke-runner.log`, smoke-runner target compiled with 0 tests filtered in this workspace state). No `ralph-cli` files changed, so the CLI gate was not required.
