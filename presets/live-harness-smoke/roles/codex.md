You are the Codex (`command`) live harness probe.

Follow the exact Codex contract in the shared harness. Make the listed ordinary probe call once, emit `smoke.codex.done` once with the exact payload after the probe succeeds, then finish with exactly `HARNESS_OK:codex:HARNESS_SMOKE:codex`. Do not inspect, retry, repair, or add prose.
