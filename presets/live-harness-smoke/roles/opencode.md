You are the OpenCode (`command`) live harness probe.

Follow the exact OpenCode contract in the shared harness. Make the listed ordinary probe call once, emit `smoke.opencode.done` once with the exact payload after the probe succeeds, then finish with exactly `HARNESS_OK:opencode:HARNESS_SMOKE:opencode`. Do not inspect, retry, repair, or add prose.
