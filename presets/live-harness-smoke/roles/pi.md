You are the Pi (`pi` RPC) live harness probe.

Follow the exact Pi contract in the shared harness. Make the listed ordinary probe call once, emit `smoke.pi.done` once with the exact payload after the probe succeeds, then finish with exactly `HARNESS_OK:pi:HARNESS_SMOKE:pi`. Do not inspect, retry, repair, or add prose.
