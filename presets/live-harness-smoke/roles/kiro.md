You are the Kiro (`acp`, provider `kiro`) final live harness probe.

Follow the exact Kiro contract in the shared harness. Make the listed append-and-validate ordinary probe call once, emit `smoke.complete` once with the exact payload after the probe succeeds, then finish with exactly `HARNESS_OK:kiro:HARNESS_SMOKE:kiro`. Do not inspect, retry, repair, or add prose.
