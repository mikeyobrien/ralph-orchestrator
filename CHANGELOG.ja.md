# 変更履歴

ralph-orchestrator への注目すべき変更はすべてここに記録されます。

形式は [Keep a Changelog](https://keepachangelog.com/en/1.1.0/) に基づいており、この
プロジェクトは [セマンティックバージョニング](https://semver.org/spec/v2.0.0.html) に
従います。

## [2.10.1] - 2026-06-22

### 修正

- カセットベースのカスタムバックエンドに標準入力経由でプロンプトを渡すことで、e2e の
  モックモード検証を復旧しました。
- モック e2e の単一イテレーションシナリオを、旧来のスクラッチパッド書き込みではなく完了
  シグナルを検証するよう更新しました。
- gitignore の統合テストを、ユーザー/グローバルの Git 除外ファイルから分離しました。

## [2.10.0] - 2026-06-21

### 追加

- Forge CLI バックエンドの統合。
- コントロールプレーンにおける RObot（ヒューマンインザループ）RPC ドメイン。加えて、
  ライブソケットではなくファイルを通じて人間との対話を駆動するファイルベースの Web モード。
- ワークツリーループがリモートのレビューブランチを公開できるようになり、リベースにも対応。
- 事前確認におけるローカルのファイルベースのハットインポート。インポートの解決と、
  インポート元ファイルのコンテキストを伴うスキーマ検証を含む。
- TUI のイテレーションバッファをディスクへオンデマンドでエクスポート: `e` は現在の
  イテレーションを、`E` はすべてのイテレーションをエクスポート。
- TUI に表示される、コンテキストウィンドウ使用率のテレメトリ。

### 変更

- Pi バックエンドのパッケージ参照を `@earendil-works/pi-coding-agent` に更新しました。
- エージェント波の可観測性ドキュメントを刷新し、ハットインポートの設計スペックを追加
  しました。

### 修正

- TUI の検索モードは、入力されたクエリ文字をキーバインドに漏らすのではなく取り込むように
  なりました（例: クエリ中の `e` がエクスポートを起動しなくなった）。`Search:` プロンプト、
  `N/M` の一致カウンタ、`n`/`N` のナビゲーションが再び機能します。
- ランタイムはガイダンスの後に明示的な完了シグナルを要求するようになり、ループの早期終了を
  防止します。
- ループの継続状態をイテレーションをまたいで永続化します。
- 生成される指示において、ハットごとのスクラッチパッド設定を尊重します。
- 終了前に ACP ターミナル出力を排出し、最終出力が切り詰められないようにします。
- パスの不一致エラーを避けるため、Ralph の成果物パスを正規化します。
- API 上で公開される MCP ツールスキーマの重複を排除します。

## [2.9.3] - 2026-05-08

### 追加

- `llms.txt` 駆動の内省のための `ralph-docs` スキル。
- `-H <name>` 配下の統一プリセット機構。YAML と TOML の両プリセットに対応。
- kiro-acp バックエンド向けの `ralph plan` 対応。完了時のフォールバックを含む。
- ライブなエージェント観察を伴うビジュアルワークフローエディタ（ビルダー）。
- ハットごとのスクラッチパッド設定。
- ハットがキャンセルの約束（cancellation promise）を設定できるように。
- ループバナー、イテレーションごとのフッター、再開ヒントを備えた `--no-tui` の UX 改善。

### 変更

- Amazon Linux 2 / 2023 との互換性のため、Linux の配布ターゲットを musl に切り替えました。
- フック用のグローバルなユーザー設定を文書化し、FAQ セクションを追加し、TOML プリセット
  形式の autoloop 作成ガイドをリンクしました。

### 修正

- Claude の子セッションは既定で `--setting-sources project,local` を使うようになり、
  ホストのユーザーレベルの `~/.claude/settings.json` のフック、プラグイン、MCP サーバーが
  Ralph のオーケストレーション実行に漏れ込むのを防ぎます。旧来の挙動を望むユーザーは
  `cli.args: ["--setting-sources", "user,project,local"]` で元に戻せます。
- テキスト完了のフォールバックを `check_completion_event()` を通すようにしました。
- `RALPH_EVENTS_FILE` 環境変数を伝播し、遅延した終了理由を保持します。
- 残存するテキストバックエンドがループの進行を停滞させるのを防ぎます。
- Rust 1.95 の `duration_suboptimal_units` clippy 警告を解消します。

### セキュリティ

- `sync-embedded-files.sh` において `source` を安全な変数パーサに置き換えました。
- `test-fresh-install.sh` で `npm install` ではなく `npm ci` を使用します。

## [2.9.2] - 2026-04-10

### 変更

- 壊れたリリース成果物を素早く検出するため、公開後の `cargo install` スモークテストを
  リリースワークフローに追加しました。
- Pi と Roo バックエンド向けに `ralph doctor` の認証環境変数の検出を拡張しました。
- バックエンドガイドで Pi バックエンドを文書化しました。

### 修正

- 非対話的な PTY 実行における PTY 標準入力のデッドロックを回避し、大きなプロンプトでの
  Claude + TUI の空白/停止出力を修正しました。

## [2.9.1] - 2026-04-04

### 修正

- `ralph-adapters` の `tool_preview.rs` を `ralph-tui` にクレートをまたいで `#[path]`
  で include していたため `cargo install ralph-cli` が失敗する問題。`tool_preview` を
  `ralph-adapters` の公開モジュールに移し、適切な依存として追加しました。

## [2.9.0] - 2026-04-03

### 追加

- ループ内での並列ハット実行のためのエージェント波。
- TUI ヘッダーでの現在のブランチ表示。
- より豊富な Copilot JSON ストリームイベントの処理。

### 変更

- リッチな TUI ツール出力とエンドツーエンドのハーネスを改善しました。
- カバレッジと Rust ゲートの検証を CI の早い段階に移し、ローカル/GitHub のチェックを
  整合させました。
- ドキュメントのリンクとカバレッジ公開の仕組みを刷新しました。

### 修正

- TUI のマウストグルの挙動。
- ループ状態のスクラッチパッドパスの解決。
- 引数ベースのバックエンドの大きなプロンプトの受け渡しを、一時ファイル経由に変更。
- CLI エグゼキュータのストリーミングと非活動タイムアウトの処理。
- 自己ループのイベント公開ガイダンスとバックエンド契約検証のカバレッジ。

## [2.8.1] - 2026-03-16

### 変更

- 2.8.1 リリースのためのバージョン更新。

## [2.8.0] - 2026-03-10

### 追加

- Ralph を stdio 上のワークスペーススコープ MCP サーバーとして公開する `ralph mcp serve`。
- ユーザースコープの既定設定の探索と、ユーザーごとの Ralph 既定値のサポート。
- TUI ヘッダーでの更新可否の通知。
- 人間のガイダンスがクリーンな再起動要求フローを起動できるように。

### 変更

- コアのプリセット群を、維持されているワークフローに集約し、プリセットのドキュメント、例、
  評価ツールを刷新しました。
- Ralph 固有のノイズを減らし引き継ぎの品質を高めるため、PDD とコードタスクのガイダンスを
  洗練しました。

### 修正

- マルチハットのプリセットイベント契約、遅延イベントの復旧、アクティブハットの表示、下流の
  デバッグ/レビューの引き継ぎを堅牢化しました。
- ハット使用時に、コア設定のランタイム上限を保持するようにしました。
- ヘッドレスループランナーのバックエンド選択を修正しました。
- 再起動の再開が必須の単一コマンドシェルフローを使うようにし、その契約カバレッジを追加
  しました。

## [2.7.0] - 2026-03-06

### 追加

- プロジェクトごとのオーケストレーターのライフサイクルフック v1。
- ACP エグゼキュータ対応の `kiro-acp` バックエンド。
- JSON-RPC の標準入出力上のサブプロセス TUI。
- ACP ベースのフロー向けの TUI ツール描画の改善。

### 変更

- 冗長なクローンを削除し `now_ts` の重複を排除して、内部のコード経路を簡素化しました。
- 非推奨の `Duration` メソッドの使用を `from_secs` に置き換えました。
- `ralph plan` の PDD SOP は、正規の `strands-agents/agent-sop` 上流ソースから同期する
  ようになり、小さな Ralph 固有のループ引き継ぎの追補を伴います。
- SOP 保守のための、埋め込みアセットの同期・チェック・上流リフレッシュのヘルパを追加
  しました。
- プリセットのドキュメントを統一し近代化しました。
- `llms.txt` マップの生成と CI 検証を追加しました。
- Web の `tsx` 事前確認の挙動を堅牢化し、資金提供のメタデータを追加しました。

### 修正

- サブプロセス TUI モードでの自己ロックの競合を回避します。
- TUI において Pi のテキストデルタを流れる段落に蓄積します。
- ゾンビ化したワークツリーループをより確実に片付けます。
- ACP の孤立プロセス、乱れた TUI 出力、欠落したツール詳細を修正します。
- clippy の問題と欠落した構造体フィールドを解消します。

## [2.6.0] - 2026-02-25

### 追加

- Rust RPC v1 のコントロールプレーンと、新しい RPC 契約への Web クライアントの移行。
- `ralph` CLI のシェル補完のサポート。
- 強制的なレビューパスを備えた `fresh-eyes` プリセット。

### 修正

- ハット表示が前のイテレーションのハットで固まらなくなりました。
- マルチバイト文字でのパニックを防ぐ UTF-8 安全な切り詰め。
- カスタムハット（OpenCode を含む）で、ハットレベルのバックエンド省略形 `args` が尊重
  されるように。
- 非推奨の `project.*` 設定キーは、`core.*` への明確な移行ヒントとともに早期に失敗する
  ようになりました。

## [2.5.1] - 2026-02-14

### 変更

- 2.5.1 リリースのためのバージョン更新。

## [2.3.0] - 2025-01-28

### 追加

- **Web ダッシュボード（アルファ版）**: Ralph のオーケストレーションループを監視・管理する
  ための多機能な Web UI
  - React + Vite + TailwindCSS のフロントエンドと、Fastify + tRPC + SQLite の
    バックエンド
  - 両サーバー（バックエンド:3000、フロントエンド:5173）を起動する `ralph web` コマンド
  - 新規インストールのための事前確認と自動インストール
  - ポート競合の検出、ラベル付き出力、ブラウザの自動起動
  - tsc+node コンパイルによるバックエンド開発向けに Node 22 を固定
- **Hats CLI**: トポロジの可視化と AI による図の生成（`ralph hats`）
- **イベント公開ガイド**: ハットがすでにアクティブなときはトポロジ表示をスキップ
- **並列設定ゲート**: ワークツリーの生成を制御する `features.parallel` 設定オプション
- **ハットごとのバックエンド引数**: ハットレベルのバックエンド設定における `args` の対応
- **新しいプリセット**: 追加のプリセットと改善されたワークフローパターン
- **ドキュメント**: ガバナンスファイルを備えたドキュメントの再編と、README の強化

### 修正

- ハットレベルのバックエンド設定と args の上書きを尊重する
- バックエンドの開発ワークフローが ts-node ではなく tsc+node を使用する

## [2.2.5] - 2025-01-17

### 追加

- ループのマージコマンド（`ralph loop merge`）とカスタムバックエンド引数
- CLI 経由でコアフィールドを上書きする設定の対応
- 費用のかからない E2E テストのためのモックアダプタ
- CI: すべての PR/プッシュでモック E2E テストを実行

### 修正

- claude-code-action のフォーク PR バグに対する CI の回避策
- フォーク PR を扱うための CI の書き込み権限

## [2.2.4] - 2025-01-14

### 修正

- npx のプロセスグループ下での TUI ハング
- サブスクリプションユーザー向けに費用表示を見積もりとして明確化

## [2.2.3] - 2025-01-12

### 追加

- git ワークツリーによるマルチループの並行実行
- 目標のドリフトを防ぐための、プロンプト内の OBJECTIVE セクション
- Claude Code の GitHub ワークフロー

### 修正

- イベント出力での UTF-8 切り詰めパニック

### 変更

- プリセット設定を更新

## [2.2.2] - 2025-01-10

### 修正

- シグナルハンドラの登録を TUI 初期化の後に移動
- ドキュメント: バッジ描画のための div への markdown 属性

## [2.2.1] - 2025-01-08

### 追加

- CLI の使い勝手: バックエンドフラグ、組み込みプリセット、URL 設定
- v2 向けの包括的な MkDocs ドキュメントサイト

### 修正

- TUI: TUI 有効化のために標準入力が端末であることを要求
- MkDocs の strict ビルドの失敗
- Confession-loop プリセットを `ralph emit` コマンドを使うよう更新

### 変更

- コードベースをモジュール化し、TUI モードを修正

[Unreleased]: https://github.com/mikeyobrien/ralph-orchestrator/compare/v2.10.1...HEAD
[2.10.1]: https://github.com/mikeyobrien/ralph-orchestrator/compare/v2.10.0...v2.10.1
[2.10.0]: https://github.com/mikeyobrien/ralph-orchestrator/compare/v2.9.3...v2.10.0
[2.9.3]: https://github.com/mikeyobrien/ralph-orchestrator/compare/v2.9.2...v2.9.3
[2.9.2]: https://github.com/mikeyobrien/ralph-orchestrator/compare/v2.9.1...v2.9.2
[2.9.1]: https://github.com/mikeyobrien/ralph-orchestrator/compare/v2.9.0...v2.9.1
[2.9.0]: https://github.com/mikeyobrien/ralph-orchestrator/compare/v2.8.1...v2.9.0
[2.8.1]: https://github.com/mikeyobrien/ralph-orchestrator/compare/v2.8.0...v2.8.1
[2.8.0]: https://github.com/mikeyobrien/ralph-orchestrator/compare/v2.7.0...v2.8.0
[2.7.0]: https://github.com/mikeyobrien/ralph-orchestrator/compare/v2.6.0...v2.7.0
[2.6.0]: https://github.com/mikeyobrien/ralph-orchestrator/compare/v2.5.1...v2.6.0
[2.5.1]: https://github.com/mikeyobrien/ralph-orchestrator/compare/v2.5.0...v2.5.1
[2.3.0]: https://github.com/mikeyobrien/ralph-orchestrator/compare/v2.2.5...v2.3.0
[2.2.5]: https://github.com/mikeyobrien/ralph-orchestrator/compare/v2.2.4...v2.2.5
[2.2.4]: https://github.com/mikeyobrien/ralph-orchestrator/compare/v2.2.3...v2.2.4
[2.2.3]: https://github.com/mikeyobrien/ralph-orchestrator/compare/v2.2.2...v2.2.3
[2.2.2]: https://github.com/mikeyobrien/ralph-orchestrator/compare/v2.2.1...v2.2.2
[2.2.1]: https://github.com/mikeyobrien/ralph-orchestrator/compare/v2.2.0...v2.2.1
